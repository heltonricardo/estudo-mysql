UPDATE ESTADOS
SET NOME = 'Maranhão'
WHERE SIGLA = 'MA';

SELECT EST.NOME
FROM ESTADOS EST
WHERE SIGLA = 'MA';

UPDATE ESTADOS
SET
	NOME = 'Paraná',
    POPULACAO = 11.32
WHERE SIGLA = 'PR';

SELECT
	NOME,
    SIGLA,
    POPULACAO
FROM ESTADOS
WHERE SIGLA = 'PR';